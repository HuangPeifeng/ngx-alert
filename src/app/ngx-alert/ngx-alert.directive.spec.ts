import { NgxAlertDirective } from './ngx-alert.directive';

describe('NgxAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxAlertDirective();
    expect(directive).toBeTruthy();
  });
});
